apiVersion: data.packaging.carvel.dev/v1alpha1
kind: Package
metadata:
  name: midnight-plugins.tanzu.vmware.com
  namespace: midnight-plugins
spec:
  refName: midnight-plugins.tanzu.vmware.com
  version: 1.0.0
  licenses:
    - "Apache 2.0"
  template:
    spec:
      fetch:
        - imgpkgBundle:
            image: # Get this value by pushing to your OCI Registry
      template:
        - ytt:
            paths:
              - config/
        - kbld:
            paths:
              - "-"
              - .imgpkg/images.yml
      deploy:
        - kapp: {}
